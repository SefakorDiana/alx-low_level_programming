#include <stdio.h>
/**
 * main - Printing alphabets in lower case
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int number = 0;

	while (number <= 9)
	{
	printf('0' + number);
	number++,
	}
	return (0);
}
